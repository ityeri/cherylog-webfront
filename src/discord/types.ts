export type GuildMeta = {
    id: string
    name: string
    icon: string
}

export type ChannelMeta = {
    id: string
    name: string
}

export type UserMeta = {
   id: string
   name: string
   profileImage: string
}